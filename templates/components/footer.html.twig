<footer class="bg-dark text-light mt-5">
    <div class="container-fluid py-5">
        <div class="row">
            <!-- Помощь -->
            <div class="col-md-3 mb-4">
                <h6 class="text-uppercase fw-bold mb-3">Помощь и поддержка</h6>
                <ul class="list-unstyled">
                    <li class="mb-2"><a href="{{ path('delivery_info', {_locale: 'ru'}) }}" class="text-light text-decoration-none">Доставка</a></li>
                    <li class="mb-2"><a href="{{ path('home_hub', {_locale: 'ru'}) }}#faq" class="text-light text-decoration-none">Возврат</a></li>
                    <li class="mb-2"><a href="{{ path('home_hub', {_locale: 'ru'}) }}#faq" class="text-light text-decoration-none">Размеры</a></li>
                    <li class="mb-2"><a href="{{ path('home_hub', {_locale: 'ru'}) }}#faq" class="text-light text-decoration-none">Помощь</a></li>
                    <li class="mb-2"><a href="{{ path('account_orders') }}" class="text-light text-decoration-none">Отследить заказ</a></li>
                </ul>
            </div>

            <!-- О компании -->
            <div class="col-md-3 mb-4">
                <h6 class="text-uppercase fw-bold mb-3">О WEARBASE</h6>
                <ul class="list-unstyled">
                    <li class="mb-2"><a href="{{ path('home_hub', {_locale: 'ru'}) }}" class="text-light text-decoration-none">О нас</a></li>
                    <li class="mb-2"><a href="{{ path('home_hub', {_locale: 'ru'}) }}#cities" class="text-light text-decoration-none">Бренды из регионов</a></li>
                    <li class="mb-2"><a href="{{ path('landing_for_brands', {_locale: 'ru'}) }}" class="text-light text-decoration-none">Для брендов</a></li>
                </ul>
            </div>

            <!-- Каталог -->
            <div class="col-md-3 mb-4">
                <h6 class="text-uppercase fw-bold mb-3">Каталог</h6>
                <ul class="list-unstyled">
                    <li class="mb-2"><a href="{{ path('brand_index', {_locale: 'ru'}) }}" class="text-light text-decoration-none">Все бренды</a></li>
                    <li class="mb-2"><a href="{{ path('catalog_index') }}" class="text-light text-decoration-none">Все товары</a></li>
                    <li class="mb-2"><a href="{{ path('catalog_index', {sale: 1}) }}" class="text-light text-decoration-none">Скидки</a></li>
                    <li class="mb-2"><a href="{{ path('catalog_index', {sort: 'new'}) }}" class="text-light text-decoration-none">Новинки</a></li>
                </ul>
            </div>

            <!-- Подписка -->
            <div class="col-md-3 mb-4">
                <h6 class="text-uppercase fw-bold mb-3">Будьте в курсе</h6>
                <p class="small">Подпишитесь на рассылку и получите 10% скидку на первый заказ</p>
                <form class="mb-3" method="post" action="{{ path('newsletter_subscribe') }}">
                    <div class="input-group">
                        <input type="email" name="email" required class="form-control" placeholder="Ваш email" aria-label="Email">
                        <input type="hidden" name="source" value="newsletter-footer">
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('newsletter_subscribe') }}">
                        <button class="btn btn-outline-light" type="submit">
                            <i class="fas fa-arrow-right"></i>
                        </button>
                    </div>
                    {# 38-ФЗ: рекламная рассылка только с предварительного согласия #}
                    <div class="form-check mt-2">
                        <input class="form-check-input" type="checkbox" required id="footerNewsletterConsent">
                        <label class="form-check-label" for="footerNewsletterConsent" style="font-size:.7rem">
                            Соглашаюсь на получение рекламной рассылки и
                            <a href="{{ path('personal_data_consent', {_locale: 'ru'}) }}" target="_blank" class="text-light">обработку персональных данных</a>
                        </label>
                    </div>
                </form>
                <div class="d-flex gap-3">
                    <a href="https://t.me/wearbase" target="_blank" rel="noopener" class="text-light"><i class="fab fa-telegram fa-lg"></i></a>
                    <a href="https://instagram.com/wearbase" target="_blank" rel="noopener" class="text-light"><i class="fab fa-instagram fa-lg"></i></a>
                </div>
            </div>
        </div>

        <!-- Нижняя часть -->
        <div class="row pt-4 border-top border-secondary">
            <div class="col-md-6">
                <p class="small mb-0">&copy; {{ "now"|date("Y") }} WEARBASE. Все права защищены.</p>
            </div>
            <div class="col-md-6 text-md-end">
                <div class="d-flex flex-wrap justify-content-md-end gap-3 small">
                    <a href="{{ path('home_hub', {_locale: 'ru'}) }}" class="text-light text-decoration-none">О нас</a>
                    <a href="{{ path('landing_for_brands', {_locale: 'ru'}) }}" class="text-light text-decoration-none">Для брендов</a>
                    <a href="{{ path('privacy_policy', {_locale: 'ru'}) }}" class="text-light text-decoration-none">Конфиденциальность</a>
                    <a href="{{ path('terms_of_use', {_locale: 'ru'}) }}" class="text-light text-decoration-none">Соглашение</a>
                    <a href="{{ path('personal_data_consent', {_locale: 'ru'}) }}" class="text-light text-decoration-none">Обработка ПД</a>
                    <a href="{{ path('cookies_policy', {_locale: 'ru'}) }}" class="text-light text-decoration-none">Cookie</a>
                </div>
            </div>
        </div>
    </div>
</footer>
