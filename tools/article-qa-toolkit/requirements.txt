# Ядро тулкита работает на чистой стандартной библиотеке Python 3.8+ —
# для базовых проверок и LLM-судьи НИЧЕГО устанавливать не нужно.
#
# Опционально (расширенные проверки):
#   language-tool-python   # грамматика/орфография (требует Java); см. qa/grammar (не обязателен)
#
# Установка опционального:
#   pip install language-tool-python
