# Бенчмарк LLM-моделей для генерации описаний брендов

Дата: 2026-06-15. Метод: 5 grounded-брендов × 5 прогонов на модель (25 сэмплов на модель).
Слепой скоринг — article-QA toolkit (text-only, модель не знает). Скорость — из ollama-метрик.
Железо: 192.168.0.119 — 4 GPU (3×RTX 2060 SUPER 8ГБ + RTX A4000 16ГБ = 40ГБ VRAM, PCIe x1 экспандер).

- **overall/human/spam** — метрики article-QA, СТИЛЕВОЕ качество (выше overall/human = лучше, ниже spam = лучше)
- **ground** — заземлённость 0–100 (слепой судья qwen3.5:27b: доля утверждений, подтверждённых RAG-контекстом; ловит галлюцинации) — **главная метрика качества**
- **TTFT** — до первого токена (тёплая модель), **tok/s** — скорость, **total** — полное время, **cold** — разовая загрузка в VRAM (по x1)

| модель | overall | human | spam | ground | words | pass% | TTFT,с | tok/s | total,с | cold,с | VRAM,ГБ |
|---|---|---|---|---|---|---|---|---|---|---|---|
| qwen3.5:27b | 86.3 | 8.59 | 8.84 | 94 | 384 | 96% | 6.2 | 12.2 | 88.9 | 124 | 28.5 |
| qwen3.6:27b | 86.1 | 8.49 | 8.92 | 98 | 420 | 88% | 5.3 | 12.8 | 67.3 | 124 | ? |
| gemma4:26b | 87.5 | 8.85 | 7.76 | 97 | 302 | 92% | 0.6 | 34.5 | 19.5 | 121 | ? |
| qwen3.6:35b | 86.5 | 8.50 | 8.44 | 95 | 331 | 92% | 2.4 | 21.0 | 32.7 | 143 | ? |
| qwen3.5:35b | 86.6 | 8.16 | 8.44 | 92 | 322 | 84% | 2.4 | 20.3 | 33.3 | 147 | ? |

> **gemma4:31b** — не запускается на этом хосте (исключена). Мультимодальная (60 текст-слоёв + 27 vision). Зажата: num_gpu=60 (всё на GPU) → «layout cannot be allocated» (не пакуется на разнокалиберные карты, 8-ГБ малы); любой CPU-offload → требует 11–16 ГБ system RAM, а у хоста (Celeron) ~9 ГБ. Стена — системная RAM, не VRAM. Для текстовых описаний vision-башня всё равно лишняя.

## Вердикт (приоритет — качество: ground → overall/human/spam → pass%)

**Победитель — `gemma4:26b`:** лучший overall (87.5), human (8.85), spam (7.76, ниже = лучше), grounding 97 (≈ топ), pass 92%. Бонус: ×3 скорость (34 tok/s), влезает в одну A4000.

- **qwen3.6:27b** — чемпион по grounding (98), но ниже overall (86.1) и pass% (88). Если grounding — ЕДИНСТВЕННЫЙ критерий, он; иначе уступает gemma.
- **35b-версии НЕ оправдались**: overall ~86.5, ground 92–95 — не лучше 27b/26b, при этом крупнее/медленнее. Больше ≠ качественнее на нашей задаче.
- Оговорка: судья grounding — qwen3.5:27b (возможен лёгкий уклон к Qwen); тем не менее gemma набрала 97 — она реально хорошо заземлена.

---

## NVIDIA Nemotron 3 — раунд 2026-06-22

Повод: проверить новое семейство NVIDIA. Тот же метод и тот же судья (qwen3.5:27b локально → сравнение с раундом выше честное). Локальные модели мерены как обычно; облачные — через флаг `app:bench:models --api` (OpenRouter, reasoning off), перф (TTFT/tok-s/cold) не замеряется — чужой GPU.

**Локальные (ollama на .119):**

| модель | overall | human | spam | ground | words | pass% | TTFT,с | tok/s | total,с | cold,с | VRAM,ГБ |
|---|---|---|---|---|---|---|---|---|---|---|---|
| nemotron-3-nano:30b | 84.8 | 8.16 | 6.96 | **68** | 215 | 60% | 2.3 | **44.1** | 14.9 | 134 | ? |

**Облачные (OpenRouter `--api`, перф не меряется):**

| модель | overall | human | spam | ground | words | pass% |
|---|---|---|---|---|---|---|
| nvidia/nemotron-3-super-120b-a12b:free | 85.4 | 8.14 | 7.24 | 90 | 264 | 60% |
| nvidia/nemotron-3-ultra-550b-a55b:free | ~83\* | — | — | — | ~280 | — |

\* Ultra — частичный прогон: free-tier OpenRouter половину вызовов отбивал «Syntax error» (не-JSON, перегрузка). 3 валидных сэмпла: overall 80.3 / 82.4 / 88.7 — уже ниже gemma; прогон прерван как нерепрезентативный.

> **Llama-3.3-Nemotron-Super-49B-v1.5 (q4_K_M, 30ГБ)** — не запускается на этом хосте (исключена), та же стена, что у gemma4:31b: `requires more system memory (20.9 GiB) than available (9.1 GiB)`. 49B не пакуется ровно на разнокалиберные карты (3×8+16ГБ) → ollama планирует CPU-offload → упирается в ~9ГБ системной RAM хоста (Celeron). `num_gpu:999` (всё на GPU) делает хуже — требует 52ГБ host RAM на стейджинг. Стена — системная RAM, не VRAM. q3_K_M (24ГБ) почти наверняка тоже за порогом.

### Вердикт раунда: чемпион не сменился

`gemma4:26b` устоял против всей доступной линейки NVIDIA:
- **nemotron-3-nano:30b** — быстрая (44 tok/s, MoE 3.5B активных), но **ground 68** (gemma 97): ~треть утверждений не подтверждена контекстом. Eyeball подтвердил — выдумывает демографию, сертификаты, «50 элементов в комплекте». 3.5B активных мало для grounded-генерации. Мимо.
- **Super 120B (API)** — несмотря на 120B и неплохой русский: overall 85.4, ground 90, pass 60% — хуже gemma по всем ключевым; code-switching (англ. вкрапления «inclusivity», «Vkontakte») бьёт по human.
- **Ultra 550B (API)** — тренд тот же (ниже), плюс free-tier нестабилен для чистого прогона.
- Снова **больше ≠ качественнее**: ни облачный 120B/550B, ни локальный 30B-MoE не обошли локальную 18-гиговую gemma. Русский у Nemotron официально не заявлен — и на grounded-задаче это видно (галлюцинации/кальки).
- Универсальное ограничение хоста: реально работают модели ~18–24ГБ, что пакуются на карты без CPU-offload; 30ГБ+ упираются в 9ГБ системной RAM. Снять — только железом (RAM хосту), не софтом.
